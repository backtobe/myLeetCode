/*
 * @lc app=leetcode.cn id=8 lang=javascript
 *
 * [8] 字符串转换整数 (atoi)
 */

// @lc code=start
/**
 * @param {string} str
 * @return {number}
 */
var myAtoi = function(str) {
// if()
  str = str.trim()
  // const first = ['-']
  if(!/^('-')|\d/.test(str)) return 0
  
};
// @lc code=end

